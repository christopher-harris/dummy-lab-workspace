# platform

Holds the PrimeNG providers and configuration for the workspace.

Import path: `@dummy-lab/platform`

## Running unit tests

Run `nx test platform` to execute the unit tests.

The library is empty for now, so its `test` target sets `passWithNoTests` in
`project.json`. Drop that option once the first spec lands.
